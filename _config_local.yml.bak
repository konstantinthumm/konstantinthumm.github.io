# Lokale Overrides
theme: minimal-mistakes-jekyll

# Liste hier alle Plugins OHNE jekyll-remote-theme
plugins:
  - jekyll-include-cache
  - jekyll-sitemap
  - jekyll-feed
  # …(deine übrigen Plugins, aber NICHT jekyll-remote-theme)

# optional: explizit leeren, damit remote_theme nicht greift
remote_theme:
